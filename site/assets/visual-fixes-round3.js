(()=>{
 'use strict';
 function guardian(root=document){root.querySelectorAll('img').forEach(img=>{const src=String(img.getAttribute('src')||'');const ctx=(img.alt||'')+' '+(img.closest('.levelBox,.levelPanel,.badgeTile,.badgeCard')?.textContent||'');if(/guardiao\.webp/i.test(src)||(/guardião/i.test(ctx)&&/\/assets\/levels\//i.test(src)))img.classList.add('round3-guardian')})}
 function center(root=document){const c=root.querySelector('.wheelCenter');if(!c)return;const imgs=[...c.querySelectorAll('img')];imgs.slice(1).forEach(x=>x.remove());if(!imgs[0]){const i=new Image();i.src='/assets/compliance-day-emblem.webp';i.alt='Emblema LG Compliance Day 2026';c.append(i)}else{imgs[0].src='/assets/compliance-day-emblem.webp';imgs[0].alt='Emblema LG Compliance Day 2026'}}
 function admin(root=document){if(!/admin\.html$/i.test(location.pathname)&&!root.querySelector('[data-admin],#adminApp,.adminShell'))return;document.body.classList.add('adminPage');const selects=[...root.querySelectorAll('select')];const phase=selects.find(s=>[...s.options].some(o=>/Fechamento/i.test(o.textContent||'')));if(phase&&!root.querySelector('.adminClosingHelp')){const note=document.createElement('div');note.className='adminClosingHelp';note.innerHTML='<strong>Fase de encerramento</strong><span>Fecha a jornada para novas etapas e consolida o resultado final. Os Compliance Champions são apurados entre participantes que concluíram as três etapas, pelo ranking oficial e pela regra de desempate configurada. Antes de encerrar, feche as etapas, revise elegíveis, exporte CSV/JSON e gere o backup.</span>';const host=phase.closest('.card,section,form,div');host?.append(note)}}
 function apply(){guardian();center();admin()}
 let q=false;new MutationObserver(()=>{if(q)return;q=true;requestAnimationFrame(()=>{q=false;apply()})}).observe(document.documentElement,{subtree:true,childList:true,attributes:true,attributeFilter:['src','class']});
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',apply,{once:true});else apply();
})();
