{
  "name": "lg-compliance-cloud",
  "version": "6.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node tests/styles.mjs && node tests/restoration.mjs && node tests/high-priority.mjs",
    "check": "npm test"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.116.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "jsdom": "30.1.0"
  }
}
