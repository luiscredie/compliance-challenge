{
  "name": "lg-compliance-cloud",
  "version": "6.0.0-p1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node tests/site.mjs && node tests/auth.mjs && node tests/activation.mjs",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.116.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "jsdom": "30.1.0"
  }
}
